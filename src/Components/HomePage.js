import React, {useState} from 'react';
import { inject, observer } from 'mobx-react';

const Homepage = (observer((props) => {
    
    return (
        <div>Welcome To Ohad's CRM Website</div>
    )
}))

export default Homepage